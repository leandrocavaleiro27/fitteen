# Copy to `.env` at project root for local Netlify dev (`npm run dev:netlify`).
# SERVER-ONLY — never commit `.env`, never prefix with VITE_.
# Production: set the same variable names in Netlify dashboard (not in chat).

SUPABASE_URL=https://rxbmsrcvvgvtulcbdwwg.supabase.co
SUPABASE_SERVICE_ROLE_KEY=
GEMINI_API_KEY=

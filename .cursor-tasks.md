# Fit Teen — Progress Tracker

**Current phase:** Phase 0–2 (local scaffold; Supabase pending credentials)  
**Overall progress:** ~75%

## Phase 0 — Scaffold & tooling
- [x] Vite + React + Tailwind + Lucide + Recharts + React Router
- [x] `netlify.toml` with functions + `/api/*` redirect
- [x] `.env.example` (client keys only; server keys documented separately)
- [ ] User creates Netlify site and connects repo

## Phase 1 — Supabase schema (WAITING: user credentials)
- [x] Local migration file `supabase/migrations/001_initial_schema.sql`
- [ ] User creates Supabase project
- [ ] User enables Google OAuth provider
- [ ] User runs migration in SQL editor ← **needed now**
- [x] User shares `VITE_SUPABASE_URL` + publishable key

## Phase 2 — Auth & profile onboarding
- [x] Google OAuth login page
- [x] Optional macro setup at registration OR skip with defaults
- [x] Display name (optional, never included in share text)
- [ ] End-to-end test with live Supabase

## Phase 3 — Netlify AI food analyzer (server-only secrets)
- [x] `netlify/functions/analyze-food.js`
- [x] `GEMINI_API_KEY` + `SUPABASE_SERVICE_ROLE_KEY` server-only
- [x] 6 scans/day limit enforced server-side
- [ ] User adds keys in Netlify dashboard
- [ ] Mobile-only camera UI on dashboard

## Phase 4 — Dashboard quick-log
- [x] Macro progress bars (aggregated from meal entries)
- [x] Sleep slider + daily notes (daily check-in)
- [x] AI food cam (mobile only, adds to totals)
- [x] Workout quick-log (multiple sessions/day)
- [ ] End-to-end test with live data

## Phase 5 — Analytics & PR tracker
- [x] Week / Month / Year tabs
- [x] Calorie vs target + sleep charts
- [x] PR board: volume = Σ(kg × reps) per exercise
- [ ] End-to-end test with live data

## Phase 6 — Social & invite
- [x] `ShareButton` — Web Share API, no user PII
- [x] Invite friends via external app link
- [ ] Test native share on mobile device

## Phase 7 — Deploy
- [ ] Connect GitHub → Netlify
- [ ] Set server env vars in Netlify (not in repo)
- [ ] Configure Supabase redirect URLs for production domain
